# Ralph Progress Log
Started: Sun Apr 19 15:50:05 +07 2026

## Codebase Patterns
- SPM packages are added directly to `DayPage.xcodeproj/project.pbxproj` using `XCRemoteSwiftPackageReference`, `XCSwiftPackageProductDependency`, and `PBXBuildFile` with `productRef` (not `fileRef`). Also add `packageReferences` to `PBXProject` and `packageProductDependencies` to `PBXNativeTarget`.
- Build command: `xcodebuild -scheme DayPage -destination 'platform=iOS Simulator,name=iPhone 17' build`
- Package resolution: `xcodebuild -resolvePackageDependencies -scheme DayPage`
- `DayPage.xcodeproj/project.xcworkspace` is in `.gitignore` — Package.resolved can't be committed directly.
- The `GeneratedSecrets.swift` is auto-generated by `scripts/generate_secrets.sh` at build time from `.env` file.

---

## 2026-04-19 - US-001
- Added `supabase-swift` SPM dependency to `DayPage.xcodeproj/project.pbxproj`
- Added `XCRemoteSwiftPackageReference` (SPM00003), `XCSwiftPackageProductDependency` (SPM00002), and `PBXBuildFile` (SPM00001) entries
- Added `packageReferences` to `PBXProject` and `packageProductDependencies` to `PBXNativeTarget`
- Supabase 2.43.1 resolved successfully with transitive deps: swift-crypto, swift-asn1, swift-http-types, swift-clocks, xctest-dynamic-overlay, swift-concurrency-extras
- **Learnings for future iterations:**
  - SPM integration in pbxproj requires 4 coordinated changes: PBXBuildFile (with productRef), PBXFrameworksBuildPhase, XCRemoteSwiftPackageReference, XCSwiftPackageProductDependency
  - Use `kind = upToNextMajorVersion; minimumVersion = 2.0.0` for semver constraint
  - Build succeeded: `** BUILD SUCCEEDED **`
---

## 2026-04-19 - US-002
- Verified `supabaseURL` and `supabaseAnonKey` already present in `GeneratedSecrets.swift`
- Verified `generate_secrets.sh` already reads `SUPABASE_URL` and `SUPABASE_ANON_KEY` from `.env`
- Verified `.env` already contains real Supabase URL and anon key values
- Verified `GeneratedSecrets.swift` listed in `.gitignore` (both `Config/` and `DayPage/Config/` paths)
- Build passes: `** BUILD SUCCEEDED **`
- **Learnings for future iterations:**
  - US-002 was already implemented as part of the Supabase secrets support (PR #86 / commit e8acf2d)
  - `GeneratedSecrets.swift` uses `enum Secrets` (not struct) — reference as `Secrets.supabaseURL`
  - Both `supabaseURL` and `supabaseAnonKey` are `String` properties inside `enum Secrets`
---

## 2026-04-19 - US-003
- Created `DayPage/Services/AuthService.swift` as `@MainActor final class AuthService: ObservableObject`
- Published properties: `session: Session?`, `isLoading: Bool`, `error: String?`
- Init creates `SupabaseClient` from `Secrets.supabaseURL` and `Secrets.supabaseAnonKey`; restores existing session asynchronously
- `signInWithApple()` stub (TODO for US-006), `signInWithMagicLink(email:)` calls `supabase.auth.signInWithOTP(email:)`, `signOut()` calls `supabase.auth.signOut()`
- Registered `AuthService.swift` in `DayPage.xcodeproj/project.pbxproj` (PBXBuildFile A10000070, PBXFileReference A20000070, Services group, PBXSourcesBuildPhase)
- Added `@StateObject private var authService = AuthService.shared` and `.environmentObject(authService)` to `DayPageApp.swift`
- Build passes: `** BUILD SUCCEEDED **`
- **Learnings for future iterations:**
  - New Swift files must be registered in `project.pbxproj` with 3 entries: PBXBuildFile, PBXFileReference, group children + PBXSourcesBuildPhase
  - `AuthService` exposes `let supabase: SupabaseClient` as internal for US-006 to access
  - `SupabaseClient` init takes `supabaseURL: URL` and `supabaseKey: String`
  - `supabase.auth.session` throws if no stored session — catch and set `session = nil`
---
