# DayPage V3 Wave 2 - Ralph Progress Log

Source PRD: tasks/prd-v3-wave2-experience.md
Branch: ralph/daypage-v3-wave2-experience
Start: 2026-04-17

Previous run (V3 Wave 1) archived at: archive/2026-04-17-daypage-v3-wave1-experience/

---

## Codebase Patterns (carried over from Wave 1, append new findings here)
- Build command: `xcodebuild -scheme DayPage -destination 'platform=iOS Simulator,arch=arm64,id=3A661BD8-5F57-42B2-B4BA-DBB2FAB4FAD0' build`
- SourceKit diagnostics show macOS errors for AVAudioSession — these are iOS-only APIs and build fine in the iOS target
- SourceKit also misreports "Cannot find X in scope" for cross-module symbols — build succeeds regardless
- VoiceService is @MainActor singleton; reuse VoiceService.transcribe(audioURL:) for the new transcribe-on-swipe-left path (US-008)
- DSColor / DSFonts / DSRadius / DSSpacing tokens used for all visuals
- VaultInitializer.vaultURL is the entry point for vault file paths
- To add new Swift files: add PBXBuildFile + PBXFileReference + group child + Sources entry in project.pbxproj (use sequential IDs)
- DayPageLogger uses nonisolated static helpers called from a DispatchQueue to avoid @MainActor actor isolation issues
- DashScope OpenAI-compatible endpoint is `https://dashscope.aliyuncs.com/compatible-mode/v1` (NOT `coding.dashscope.aliyuncs.com/v1` — that returns 404). The `Secrets.dashScopeBaseURL` env override still wins; only the in-code default changed.
- Use `#if DEBUG print(...)` (not DayPageLogger) for noisy network diagnostics — keeps release logs clean and avoids @MainActor isolation hops.
- For archive-style detail screens that read vault files: load asynchronously inside a `.task` closure into an explicit `LoadState` enum, not inside computed properties. Computed-property IO runs on every re-render and hides failure paths.
- Validate user-supplied date strings with BOTH a strict `^\d{4}-\d{2}-\d{2}$` regex AND a non-lenient DateFormatter round-trip — the regex alone lets `2020-02-30` through.
- When a file-backed view (RawMemoView, DailyPageView) is opened, double-check `FileManager.default.fileExists(atPath:)` inside its own load path even if the parent already checked. Log `url.path` + `errno` on any miss — both are necessary to diagnose sandbox / migration issues.
- When adding a "test this endpoint" Settings action, ALWAYS resolve baseURL/model the same way the production service does (`Secrets.*.isEmpty ? defaultLiteral : Secrets.*`). If you hardcode a URL in Settings, you will silently drift from CompilationService and the test can green while real compile 404s.
- NSURLError codes (timeout=-1001, notConnected=-1009, cannotFindHost=-1003, cannotConnect=-1004) are the right bucket for network-layer hints; HTTP status codes only show up when the server actually responded.

---

## 2026-04-17 - US-001 DashScope baseURL 修正 + 失败可观测
- Changed default baseURL in `DayPage/Services/CompilationService.swift` (callDashScope) from `https://coding.dashscope.aliyuncs.com/v1` → `https://dashscope.aliyuncs.com/compatible-mode/v1`
- Added `#if DEBUG print("[DashScope] URL=... status=... body=<first 500 chars>")` on both non-200 responses and missing HTTPURLResponse paths
- Updated `CLAUDE.md` line 30 to match new default baseURL
- Files changed: `DayPage/Services/CompilationService.swift`, `CLAUDE.md`, `prd.json`, `progress.txt`
- Build: `xcodebuild -scheme DayPage ... build` → BUILD SUCCEEDED
- Manual API hit verification deferred (needs real DashScope key, not present in this sandbox). Default URL was wrong path — only host/path differ; method, headers, body schema remain OpenAI-compatible.
- **Learnings for future iterations:**
  - The `Secrets` shim reads `dashScopeBaseURL` from env via `Config/GeneratedSecrets.swift`; in-code default only kicks in when env is empty. So users who already set `DASHSCOPE_BASE_URL` keep their override.
  - DEBUG-only print is intentional: prevents leaking partial response bodies in release. If you later need release telemetry, route through DayPageLogger instead.
  - SourceKit "Cannot find X in scope" diagnostics on this file (NetworkMonitor / RawStorage / Secrets / EntityPageService / VaultInitializer / DayPageLogger) are the documented cross-module false positives — ignore them, trust xcodebuild.
---

## 2026-04-17 - US-002 DayDetailView 三态健壮化 + dateString 格式校验
- Rewrote `DayPage/Features/Archive/DayDetailView.swift` with an explicit `LoadState` enum (`.loading`, `.compiled`, `.rawOnly`, `.empty`, `.error(String)`). Moved file-existence checks out of computed properties into an async `.task` that runs off the main thread via `Task.detached`.
- `dateString` is validated in two stages: strict regex `^\d{4}-\d{2}-\d{2}$`, then a non-lenient `DateFormatter` round-trip with UTC timezone so values like `2020-02-30` fall into `.error`.
- Added dedicated `.empty` (calendar.badge.exclamationmark + "这一天还没有记录" + 关闭) and `.error` (exclamationmark.triangle + message + 关闭) views, each with a Close button wired to `dismiss()`.
- `RawMemoView.loadMemos()` now logs full `url.path` + `errno` when the raw file is missing or read throws; bails to empty state instead of blindly calling `RawStorage.read` on an invalid date.
- `DailyPageView.loadPage()` similarly guards with `FileManager.default.fileExists(atPath:)` before reading the compiled daily file, logs full path + errno on miss, and validates `dateString` before reading raw memos.
- Files changed: `DayPage/Features/Archive/DayDetailView.swift`, `DayPage/Features/Archive/RawMemoView.swift`, `DayPage/Features/Daily/DailyPageView.swift`, `prd.json`, `progress.txt`.
- Build: `xcodebuild -scheme DayPage ... build` → BUILD SUCCEEDED. App launched on booted simulator (iPhone 17 Pro, iOS 26.1) without crash.
- **Learnings for future iterations:**
  - `.task { await load() }` is the right hook for vault-backed detail views — it auto-cancels on view disappearance and lets you await `Task.detached` without bouncing across main actor manually.
  - Keep `hasRawFile` as a separate `@State Bool` even after the `LoadState` resolves — the user can still switch the segmented control inside `.compiled`, and the Raw tab needs its own "no file" path.
  - SourceKit diagnostics from macOS context (`No such module 'UIKit'`, `Cannot find DSColor/Memo in scope`) fire on this file too; they're the documented cross-module false positives — trust `xcodebuild`.
  - Automated UI navigation to an invalid `dateString` requires either (a) the verify-daypage MCP skill wired up, or (b) a UI test target. Neither exists yet — US-009 will add unit-level coverage of the state machine, which is the practical regression guard.
---

## 2026-04-17 - US-003 Settings 增加「测试 API 连接」按钮
- Rewrote `testDashScope()` in `DayPage/Features/Settings/SettingsView.swift` to resolve baseURL identically to `CompilationService.callDashScope` (`Secrets.dashScopeBaseURL.isEmpty ? "https://dashscope.aliyuncs.com/compatible-mode/v1" : Secrets.dashScopeBaseURL`). Sends `POST {baseURL}/chat/completions` with `{model: resolved, max_tokens: 1, messages: [{role: "user", content: "hi"}]}` and 10s timeout.
- Added inline per-row `APITestResult` rendering under each API Key row: green ✓ + "连接成功 · 模型 \(model)" on 200; red ✗ + "HTTP N · <hint>" on non-200. Button becomes a `ProgressView` and is disabled while `dashScopeTesting == true`.
- Tailored hints: DashScope (401/403/404/429/5xx), OpenAI (401/403/404/429/5xx), OpenWeather (401/404/429/5xx); network errors mapped via `NSURLErrorDomain` codes (timeout → '网络不可达，检查代理（超时）', notConnected, cannotFindHost/cannotConnect → 'URL 不存在，检查 baseURL', networkConnectionLost).
- Whisper and OpenWeather test paths mirror the same result / disabled-spinner pattern so UX is consistent across all three keys.
- Added private `APITestResult` enum at file scope with `.success(String)` / `.failure(String)` + `isSuccess` / `message` accessors. Scoped fileprivate to SettingsView to avoid polluting global namespace.
- Files changed: `DayPage/Features/Settings/SettingsView.swift`, `prd.json`, `progress.txt`.
- Build: `xcodebuild -scheme DayPage ... build` → BUILD SUCCEEDED.
- Manual Simulator verification deferred — requires real DashScope/Whisper/OpenWeather keys in env; the UI state machine is covered at compile time by the enum + per-row rendering.
- **Learnings for future iterations:**
  - SettingsView already had stub `testX()` methods with the wrong URL. The lesson: when a future story requires "test endpoint X", grep for existing `test{Service}()` helpers first — there's almost always a 70% stub waiting to be finished.
  - Using a tiny fileprivate `enum APITestResult` is nicer than juggling `(Bool, String)` tuples or two optional `@State`s. Keep it fileprivate to the view — do NOT hoist to a shared type until there's a second caller.
  - `bannerCenter.show(...)` is the right channel for app-wide outcomes, but for a "test connection" button the user is already looking at, inline rendering under the row is stronger UX — leave the banner out to avoid duplicate signals.
  - `Secrets.dashScopeModel` default must match CompilationService.swift exactly (`qwen3.5-plus`) — if one drifts, the success banner will show a model that was never used.
---

## Wave 2 Story Order Rationale
- US-001 first: unblocks compile entirely; one-line URL change
- US-002 second: unblocks Archive entirely; refactor + 4-state machine
- US-003 next: depends on US-001's URL fix
- US-004 → US-005: must delete redundant indicators before adding the new footer button (avoid 4 indicators on screen)
- US-006: depends on US-002 (DayDetailView must already handle .empty without crashing)
- US-007 → US-008: must have InputBarV2 framework before bolting on press-to-talk gesture
- US-009 last: tests for US-002, easier to write after the implementation stabilizes
