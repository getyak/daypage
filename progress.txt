# DayPage iCloud Sync - Ralph Progress Log

Source PRD: tasks/design-icloud-sync.md (Issue #64)
Branch: ralph/icloud-sync
Start: 2026-04-22

Previous run (V3 Wave 2) archived at: archive/2026-04-22-daypage-v3-wave2-experience/

---

## Codebase Patterns (carried over from Wave 2)
- VaultInitializer.vaultURL is the single entry point for all 37 vault file path accesses
- RawStorage.atomicWrite uses temp-file + FileManager.replaceItemAt pattern for atomic writes
- Services are @MainActor final class singletons; view models are @MainActor final class: ObservableObject
- DSColor / DSFonts / DSRadius / DSSpacing tokens used for all visuals
- To add new Swift files: add PBXBuildFile + PBXFileReference + group child + Sources entry in project.pbxproj (use sequential IDs A1xxxxxxx / A2xxxxxxx, last used A20000056)
- Build command: xcodebuild -scheme DayPage -destination 'platform=iOS Simulator,arch=arm64,id=3A661BD8-5F57-42B2-B4BA-DBB2FAB4FAD0' build
- Test command: xcodebuild -scheme DayPage -destination 'platform=iOS Simulator,arch=arm64,id=3A661BD8-5F57-42B2-B4BA-DBB2FAB4FAD0' test
- SourceKit diagnostics show macOS/cross-module false positives — trust xcodebuild, not SourceKit
- DayPageTests target uses T* pbxproj ID prefix; app uses A* prefix
- VaultInitializer.testOverrideURL is the injection point for unit tests
- RawStorage.atomicWrite is now wrapped in NSFileCoordinator; temp-file + replaceItemAt logic runs inside the coordinator block
- iCloudVaultLocator.vaultURL falls back to LocalVaultLocator().vaultURL when iCloud unavailable (nil ubiquity container)
- VaultInitializer.shared is a lazy static var (closure-evaluated once at first access); safe to reassign later for rollback

---

## 2026-04-22 - US-001
- Implemented VaultLocator protocol with `vaultURL: URL` and `isUsingiCloud: Bool` in DayPage/Storage/VaultLocator.swift
- Implemented LocalVaultLocator struct returning Documents/vault path (identical to prior hard-coded behavior)
- Added `VaultInitializer.shared: VaultLocator = LocalVaultLocator()` static property
- Updated `VaultInitializer.vaultURL` to: `if let override = testOverrideURL { return override }; return shared.vaultURL`
- Added VaultLocator.swift to project.pbxproj (IDs A10000056/A20000056)
- Files changed: DayPage/Storage/VaultLocator.swift (new), DayPage/Storage/VaultInitializer.swift, DayPage.xcodeproj/project.pbxproj
- **Learnings for future iterations:**
  - VaultInitializer.shared is now the injection point for swapping storage backends at runtime
  - All 37 call sites remain untouched; they continue to use VaultInitializer.vaultURL which now delegates to shared.vaultURL
  - Last pbxproj ID used: A20000056 (next available: A20000057)
---

## 2026-04-22 - US-002
- Added iCloud entitlements to DayPage/App/DayPage.entitlements: icloud-container-identifiers, ubiquity-container-identifiers, icloud-services=CloudDocuments
- Implemented `struct iCloudVaultLocator: VaultLocator` in DayPage/Storage/VaultLocator.swift
  - `vaultURL`: returns ubiquity container's Documents/vault, falls back to LocalVaultLocator when nil
  - `isUsingiCloud`: returns true iff `url(forUbiquityContainerIdentifier:)` is non-nil
- Updated `VaultInitializer.shared` from `= LocalVaultLocator()` to a lazy closure: tries iCloudVaultLocator first, falls back to LocalVaultLocator
- Wrapped `RawStorage.atomicWrite` with `NSFileCoordinator.coordinate(writingItemAt:options:.forReplacing)`: temp-file + replaceItemAt logic runs inside the coordinator block
- Files changed: DayPage/App/DayPage.entitlements, DayPage/Storage/VaultLocator.swift, DayPage/Storage/VaultInitializer.swift, DayPage/Storage/RawStorage.swift
- Build: ** BUILD SUCCEEDED **
- **Learnings for future iterations:**
  - When iCloud not configured (Simulator without account), iCloudVaultLocator.isUsingiCloud == false → shared = LocalVaultLocator() → no crash
  - NSFileCoordinator.coordinate block receives a potentially remapped `coordinatedURL`; use it (not the original url) for all I/O inside the block
  - NSFileCoordinator errors land in the `error:` inout parameter, not as thrown exceptions; capture both coordinatorError and writeError
---

## 2026-04-22 - US-003
- Added `enum VaultLocation: String { case local, iCloud }` and `enum AttachmentPolicy: String { case onDemand, alwaysLocal }` to AppSettings.swift
- Added `vaultLocation: VaultLocation` property (UserDefaults key "vaultLocation", default .local) with objectWillChange.send() in setter
- Added `attachmentPolicy: AttachmentPolicy` property (UserDefaults key "attachmentPolicy", default .onDemand) with objectWillChange.send() in setter
- Added `migrationCompletedAt: Date?` property (UserDefaults key "migrationCompletedAt", default nil) with objectWillChange.send() in setter
- Files changed: DayPage/Config/AppSettings.swift
- Build: ** BUILD SUCCEEDED **
- **Learnings for future iterations:**
  - AppSettings uses objectWillChange.send() before UserDefaults.set() — no @Published needed since properties are computed (read from UserDefaults each time)
  - The pattern: `objectWillChange.send() + UserDefaults.set()` triggers SwiftUI updates for computed properties on ObservableObject
  - VaultLocation and AttachmentPolicy enums are defined at file scope in AppSettings.swift, not nested, for cleaner access in other files
---
